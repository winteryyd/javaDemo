<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:batch="http://www.springframework.org/schema/batch"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- Launch batch job thread pool -->
    <bean id="batchJobExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor"
          destroy-method="destroy">
        <property name="corePoolSize" value="5"/>
        <property name="maxPoolSize" value="10"/>
        <property name="queueCapacity" value="30"/>
    </bean>

    <!-- Persist batch metadata in database -->
    <bean id="jobRepository" class="org.springframework.batch.core.repository.support.JobRepositoryFactoryBean">
        <property name="transactionManager" ref="transactionManagerForMBI"/>
        <property name="dataSource" ref="proxyDataSourceForMBI"/>
        <property name="databaseType" value="Oracle"/>
        <property name="tablePrefix" value="#{conf['batch.table.prefix']}"/>
        <property name="isolationLevelForCreate" value="ISOLATION_READ_COMMITTED"/>
    </bean>

    <bean id="jobLauncher" class="org.springframework.batch.core.launch.support.SimpleJobLauncher">
        <property name="jobRepository" ref="jobRepository"/>
        <property name="taskExecutor" ref="batchJobExecutor"/>
    </bean>

    <bean id="stepBuilderFactory" class="org.springframework.batch.core.configuration.annotation.StepBuilderFactory">
        <constructor-arg index="0" ref="jobRepository"/>
        <constructor-arg index="1" ref="transactionManagerForMBI"/>
    </bean>

    <bean id="jobBuilderFactory" class="org.springframework.batch.core.configuration.annotation.JobBuilderFactory">
        <constructor-arg index="0" ref="jobRepository"/>
    </bean>

    <bean id="jobExplorer" class="org.springframework.batch.core.explore.support.JobExplorerFactoryBean">
        <property name="tablePrefix" value="#{conf['batch.table.prefix']}"/>
        <property name="dataSource" ref="proxyDataSourceForMBI"/>
    </bean>

    <bean id="jobRegistry" class="org.springframework.batch.core.configuration.support.MapJobRegistry"/>

    <bean id="jobRegistryBeanPostProcessor"
          class="org.springframework.batch.core.configuration.support.JobRegistryBeanPostProcessor">
        <property name="jobRegistry" ref="jobRegistry"/>
    </bean>

    <bean id="jobOperator" class="org.springframework.batch.core.launch.support.SimpleJobOperator">
        <property name="jobExplorer" ref="jobExplorer"/>
        <property name="jobRepository" ref="jobRepository"/>
        <property name="jobRegistry" ref="jobRegistry"/>
        <property name="jobLauncher" ref="jobLauncher"/>
    </bean>

    <bean id="runIdIncrementer" class="org.springframework.batch.core.launch.support.RunIdIncrementer"/>

    <bean id="jobCleaner" class="com.deppon.mbi.support.schedule.batch.JobCleaner">
        <property name="dbType" value="ORACLE"/>
        <property name="tablePrefix" value="#{conf['batch.table.prefix']}"/>
        <property name="jdbcTemplate" ref="jdbcTemplateForMBI"/>
    </bean>


    <!-- ################################################### -->
    <!--               Global Job Configuration              -->
    <!-- ################################################### -->

    <batch:job id="abstractJob"
               restartable="true"
               incrementer="runIdIncrementer"
               job-repository="jobRepository" abstract="true">
    </batch:job>

    <batch:step id="abstractStep" abstract="true" job-repository="jobRepository">
        <batch:tasklet transaction-manager="transactionManagerForMBI" allow-start-if-complete="true" throttle-limit="5">
            <batch:chunk commit-interval="#{conf['batch.job.commitInterval']}"
                         skip-limit="#{conf['batch.job.skipLimit']}"  >
                <batch:skippable-exception-classes>
                    <batch:include class="java.lang.Exception"/>
                </batch:skippable-exception-classes>
            </batch:chunk>
            <batch:listeners>
                <batch:listener ref="stepErrorListener"/>
            </batch:listeners>
        </batch:tasklet>
    </batch:step>

    <!-- ################################################### -->
    <!--              Job Component Configuration            -->
    <!-- ################################################### -->
    <bean id="stepErrorListener" class="com.deppon.mbi.support.schedule.batch.listener.StepErrorListener"/>

    <!-- 用户信息 -->
    <batch:job id="userInfoBatchJob" parent="abstractJob">
        <batch:step id="userInfo_step" parent="abstractStep">
            <batch:tasklet>
                <batch:chunk>
                    <batch:reader>
                        <bean class="org.mybatis.spring.batch.MyBatisPagingItemReader" scope="step">
                            <property name="pageSize" value="#{conf['batch.task.pageSize']}"/>
                            <property name="sqlSessionFactory" ref="sqlSessionFactoryForMBI"/>
                            <property name="queryId"
                                      value="com.deppon.mbi.module.auth.shared.domain.UserEntity.userInfoCachePush"/>
                        </bean>
                    </batch:reader>
                    <batch:processor>
                        <bean class="com.deppon.mbi.support.schedule.batch.processor.JsonConvertProcessor">
                        </bean>
                    </batch:processor>
                    <batch:writer>
                        <bean class="com.deppon.mbi.support.schedule.batch.writer.ItemToRedisWriter">
                            <property name="stringCache" ref="codisStringCache"/>
                            <property name="redisKeyBuilder">
                                <bean class="com.deppon.mbi.module.framework.server.schedule.keybuilder.UserInfoKeyBuilder"/>
                            </property>
                        </bean>
                    </batch:writer>
                </batch:chunk>
            </batch:tasklet>
        </batch:step>
    </batch:job>
</beans>