<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deppon.demo.server.dao.mybatis.IUserDao">
    
	<sql id="testSingleTableColumns">
		a.id AS "id",
		a.name AS "name",
		a.phone AS "phone",
		a.addr AS "addr"
	</sql>
	
	<sql id="testSingleTableJoins">
	</sql>
    
	<select id="get" resultType="com.deppon.demo.shared.entity.UserEntity">
		SELECT 
			<include refid="testSingleTableColumns"/>
		FROM userentity a
	</select>
	
	<insert id="insert">
		INSERT INTO userentity(
			id,
			name,
			phone,
			addr
		) VALUES (
			#{id},
			#{name},
			#{phone},
			#{addr}
		)
	</insert>
	
	<update id="update">
		UPDATE userentity SET 	
			name = #{name},
			phone = #{phone},
			addr = #{addr}
		WHERE id = #{id}
	</update>
	
	
	<update id="delete">
		DELETE FROM userentity
		WHERE id = #{id}
	</update>
</mapper>